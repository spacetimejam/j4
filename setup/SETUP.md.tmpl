# Setup incomplete: first AI session tasks

This file was written by the setup wizard on {{DATE}}. It is a to-do list for
the AI assistant's first session in this project. Work through the tasks in
order with {{USER_NAME}}, then delete this file.

## Tasks

1. Run the intake interview in `core/intake.md` and record the answers there.
2. Build `core/profile.md`, including a fit bar appropriate to someone who is
   {{EMPLOYMENT_STATUS}}: what a new role must offer before it is worth moving for.
3. Synthesise `core/master-cv.md` from {{USER_NAME}}'s existing CV. Ask them to
   drop a copy (any format) into `core/source/` first.
4. Seed `core/stories.md` and `core/voice.md` from the conversation so far.
5. Confirm the tracker works. Then set up the render templates: help
   {{USER_NAME}} choose a Typst Universe CV template
   (https://typst.app/universe), or replicate their existing CV design from an
   example they provide, and vendor it into `render/templates/` so `main.typ`
   and `cover-letter.typ` exist and `render.sh` works end to end; verify with
   a real render of a dummy slug.
6. Delete this file and tell {{USER_NAME}} the project is ready to judge fits
   and draft applications.

## Wizard answers

- Name: {{USER_NAME}}
- Email: {{USER_EMAIL}}
- Phone: {{USER_PHONE}}
- Location: {{USER_LOCATION}}
- Field: {{FIELD}}
- Seniority: {{SENIORITY}}
- Employment status: {{EMPLOYMENT_STATUS}}
- AI tool: {{AI_TOOL}}
- Tracker: {{TRACKER}}
- Portal: {{PORTAL}}
- Creative module: {{CREATIVE}}
- Setup date: {{DATE}}
